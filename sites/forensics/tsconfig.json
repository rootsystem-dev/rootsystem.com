{
  "extends": "astro/tsconfigs/strict",
  // See sites/www/tsconfig.json — worker-configuration.d.ts is generated by
  // `wrangler types` and supplies the `cloudflare:workers` module types.
  "include": [".astro/types.d.ts", "worker-configuration.d.ts", "**/*"],
  "exclude": ["dist"]
}
